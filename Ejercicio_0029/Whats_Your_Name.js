function message(first, last) {
    alert("Hello "+first+" "+last+"! You just delved into JavaScript.")
}

const first = prompt("\nFirst name:\n");
const last = prompt("\nLast name:\n");
message(first, last)